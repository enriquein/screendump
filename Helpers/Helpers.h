#ifndef MY_HELPERS
#define MY_HELPERS
// void SetTransparency(HWND hWnd, long lAlpha); 
//CString GetNewFilename(); // Returns a string with the automatically generated filename.
//BOOL CheckCreateDir(CString path); // Returns true if the Dir exists, False if it wasn't found AND I wasn't able to create it.
//CString GetFilenameFromUser(); // Returns the selected filename. If user cancels out, then it returns an empty string.
#endif
